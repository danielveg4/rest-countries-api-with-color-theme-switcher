import {StyledHeader} from './styles';

const Header = () => {
    return (
        <StyledHeader>
            <h1>Where in the world?</h1>
            <button>Dark Mode</button>
        </StyledHeader>
    );
};

export default Header;